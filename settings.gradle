
rootProject.name = 'kotlinq'
include 'core'
include 'dsl'
include 'jvm'
include 'schema'

[':core',
 ':dsl',
 ':jvm',
 ':schema'].stream()
    .map({ project(it) })
    .map({ [it, "kotlinq-${it.name}"] })
    .forEach({ it[0].name = it[1] })
